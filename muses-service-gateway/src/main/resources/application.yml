spring:
  application:
    name: muses-gateway

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    service-url:
      #defaulutZone: http://${eureka.instance.hostname}:8761/eureka/
      defaultZone: http://master:8001/eureka/,http://slaver:8002/eureka/

zuul:
  routes:
    sale:
      path: /sale/**
      # 路由的服务器Id
      service-id: invoker     
#zuul:
 # routes:
    # Map
    # this is key
  #  books:
      #the value
   #   url: http://localhost:8090